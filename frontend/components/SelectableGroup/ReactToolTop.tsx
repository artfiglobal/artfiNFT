import React from "react";
import ReactTooltip from "react-tooltip";

const ReactToolTop = () => {
  return (
    <ReactTooltip
      anchorId="my-anchor-element"
      content="hello world"
      place="top"
    />
  );
};

export default ReactToolTop;
